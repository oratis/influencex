# npm resolves per-project config from the nearest package.json directory,
# so the repo-root .npmrc does NOT apply here. npm 10 (Node 20 CI/runtime)
# fails `npm ci` on vitest's optional esbuild peer range without this;
# the Dockerfile passes --legacy-peer-deps explicitly for the same reason.
legacy-peer-deps=true
